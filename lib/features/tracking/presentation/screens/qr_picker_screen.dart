import 'package:flutter/material.dart';

class QrPickerScreen extends StatelessWidget {
  const QrPickerScreen({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('QR Picker')));
}
