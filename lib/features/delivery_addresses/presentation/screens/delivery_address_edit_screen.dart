import 'package:flutter/material.dart';

/// Stub — sera remplacé en Task 6 par l'implémentation complète.
class DeliveryAddressEditScreen extends StatelessWidget {
  const DeliveryAddressEditScreen({super.key, this.addressId});

  final String? addressId;

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Adresse de livraison')),
      body: const Center(child: Text('À implémenter (Task 6)')),
    );
  }
}
